{
  "name": "@markup-carve/wp-carve",
  "version": "0.1.0",
  "private": true,
  "description": "Build tooling for the in-browser Carve engine bundle used by wp-carve.",
  "license": "MIT",
  "scripts": {
    "build": "esbuild assets/js/src/engine.js --bundle --format=iife --minify --outfile=assets/js/vendor/carve.js",
    "watch": "npm run build -- --watch",
    "test:js": "node --test tests/js/*.test.mjs"
  },
  "dependencies": {
    "@markup-carve/carve": "git+https://github.com/markup-carve/carve-js.git#main"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
