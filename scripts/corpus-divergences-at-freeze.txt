# Documents the engine this plugin SHIPS renders differently from the spec
# corpus, measured at the pinned freeze.
#
#   corpus  markup-carve/carve 0ea5936b0c1c, the 0.1.5 release, 1685 documents
#   engine  markup-carve/carve-php 9fd773b, the dev-main revision composer.lock installs
#           (upstream main is exactly the 0.1.7 release commit)
#
# Both sides are fixed revisions, so this list is the whole result and not a
# snapshot of one. A NAME APPEARING HERE is a divergence introduced by a change
# in this repository, because there are none to inherit: the list is EMPTY, and
# 0 of 1685 documents render differently.
#
# BOTH ENDS MOVED TOGETHER AGAIN for carve-php 0.1.7 and carve 0.1.5. Left at
# the 0.1.4 corpus, the newer engine reported `06-task-lists-2.crv` as a
# divergence introduced by this repository, when it was the spec's own
# task-state ruling (carve#1866) reaching the engine before the corpus that
# pins it. Moving one end alone measures the distance between the pins.
#
# It was 195 names against the previous freeze (carve 0f6b9903 of 18 August,
# 1245 documents, engine 0.1.4). Nothing here fixed them. Both ends moved to a
# release at the same time, which is what the corpus job's header asks for when
# carve-php publishes, and the residual the old pair measured turned out to be
# the distance between the two pins rather than anything unimplemented.
#
# An empty list is a stronger gate than a populated one, so keep it empty: a
# single name appearing means this repository changed rendering, with no
# inherited noise to argue it away.
#
# The `(none)` below is not a comment. It states the zero, so that a
# baseline which failed to load - which is also a file with no names - stays
# distinguishable from one that renders clean.
(none)
